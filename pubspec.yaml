name: smart_waste_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.7.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.18.0 # Updated to the latest compatible version of firebase_core
  firebase_auth: ^4.5.0 # Keep firebase_auth as it is
  firebase_auth_web: ^5.8.13
  cloud_firestore: ^4.10.0 # Keep cloud_firestore to a compatible version
  provider: ^6.0.1 # Updated to the latest version of provider
  
  cupertino_icons: ^1.0.8
  flutter_native_splash: ^2.4.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  # Add any assets here if needed
